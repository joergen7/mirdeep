/* miRDeep Workflow
 *
 * Tools:
 *
 * Bowtie 1.1.2
 * SRAtools 2.5.7
 * FastQC 0.11.4
 * Trimmomatic 0.35
 * miRDeep 2.0.0.7
 * ViennaRNA 2.1.9
 * randfold 2.0
 */
 
deftask extract-mirna-hsa( hsa( File ) : fa( File ) )in bash *{
  extract_miRNAs.pl $fa hsa > $hsa
}*

deftask extract-mirna-mature( hsa( File ) : fa( File ) )in bash *{
  extract_miRNAs.pl $fa has mature > $hsa
}*